Write a custom importer by implementing either the `DartSass.Importer` or
`DartSass.FilesystemImporter` protocol and passing an instance wrapped in
a `DartSass.ImportResolver` to a `DartSass.Compiler`.
